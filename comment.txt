ตอนรันคือ ต้องเข้า venv environment ก่อน คำสั่งคือ source venv/bin/activate
แล้วรัน เพื่อเข้า ใช้คำสั่ง python app.py ไม่ก็คำสั่ง flask run ก็จะแสดงหน้าเว็บออกมา

psql -h localhost -U postgres -d job_market

Set up this Neon project in the current working directory.

1. `npm i -g neon@latest && neon login`
2. `neon skills -y`
3. `neon mcp -y`
4. `neon link --project-id icy-leaf-31631122 --branch production -y`
5. `neon config init`
6. Update `neon.ts`:

```ts
import { defineConfig } from "@neon/config/v1";

export default defineConfig({});
```

7. `neon deploy`

# connection string
postgresql://neondb_owner:npg_tk1JEFdRo5Ol@ep-autumn-leaf-b3r405h2-pooler.c-4.ap-southeast-1.aws.neon.tech/neondb?sslmode=require&channel_binding=require

psql postgresql://neondb_owner:npg_tk1JEFdRo5Ol@ep-autumn-leaf-b3r405h2.c-4.ap-southeast-1.aws.neon.tech/neondb?sslmode=require